## 1. 快捷键分发

- [x] 1.1 在 `src/app.rs` 增加只比较 ASCII 字母且忽略大小写的快捷键辅助函数，并为大小写、非字母和非字符事件添加单元测试
- [x] 1.2 更新主界面、查找栏和 Level 焦点的字母动作分支，使用辅助函数匹配大写和小写字符，同时保留现有修饰键限制
- [x] 1.3 更新 Devices、ExportMenu 和 Settings 等非文本弹窗的字母动作分支，使大写快捷键执行与小写相同的操作
- [x] 1.4 保留查找导航 `n` / `N` 的原始大小写分支，并确保筛选、查找、导出路径和设置文本编辑继续把大写字符交给文本输入处理

## 2. 回归测试

- [x] 2.1 增加主界面和 Find/Level 焦点的大写快捷键测试，覆盖动作触发、`Ctrl/Cmd` 组合和既有修饰键限制
- [x] 2.2 增加 Devices、ExportMenu、Settings 等弹窗的大写快捷键测试
- [x] 2.3 增加 `n` 为下一项、`N` 为上一项的查找导航测试，并验证文本输入中的大写字符原样保留

## 3. 文档与验证

- [x] 3.1 更新 `README.md` 快捷键说明，注明字母快捷键支持大小写，并保留 `n/N` 的方向区别
- [x] 3.2 运行格式检查和完整 Rust 测试，确认所有现有测试及新增测试通过
