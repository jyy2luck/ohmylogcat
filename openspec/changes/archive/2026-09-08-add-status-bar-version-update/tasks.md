## 1. 共享探测与版本比较

- [x] 1.1 将 `releases/latest` 跳转解析与标签抽取抽成 CLI 与终端界面共用的函数，命令行 `update` 后的字符串相等比较保持不变
- [x] 1.2 为探测命令加上约 5 秒超时，超时、非零退出或无法解析时返回失败，供界面静默处理
- [x] 1.3 实现仅用于界面提示的数字三段比较（忽略前导 `v` 与 `-` / `+` 后缀），无法解析则视为不更新
- [x] 1.4 为标签抽取、三段比较（更新 / 相同 / 本地更新 / 带后缀）补充单元测试

## 2. 文案与底部栏布局

- [x] 2.1 在 `UiStrings` 增加当前版本与更新后缀片段，填入英 / 简 / 繁，格式对齐设计文档对照表
- [x] 2.2 将 `draw_status` 改为三区：左侧条数、中间错误或短提示、最右侧版本簇（更新后缀仅加粗）
- [x] 2.3 实现窄宽度裁切：先丢掉更新后缀，再截断或丢掉中间提示，尽量保留当前版本；条数优先于版本簇
- [x] 2.4 为三语文案拼接、错误不覆盖版本簇、窄宽度先丢后缀补充单元测试

## 3. 启动时非阻塞探测

- [x] 3.1 终端界面构造时后台发起一次探测，经通道把结果送回，`tick` 中 `try_recv`，不阻塞首帧
- [x] 3.2 仅当远端三段更大时写入最新版本并显示后缀；失败、超时、相等或更旧都不写错误槽、不调安装脚本

## 4. 验证

- [x] 4.1 运行完整 Rust 测试，确认 CLI 生命周期现有测试与新增测试均通过
